.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/
.env
.DS_Store

# Keep the directories, ignore their contents: a run must not be committed,
# but the layout must survive a clone (the CLI writes into them).
evidence/*
!evidence/.gitkeep
out/*
!out/.gitkeep

tool/
