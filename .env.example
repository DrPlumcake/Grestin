# Copy to .env (git-ignored). Both are optional:
# without them the free endpoints (crt.sh, internetdb, cvedb, KEV, EPSS,
# ransomware.live) still work, and the run records which stages were skipped.
SHODAN_API_KEY=
OPENSANCTIONS_API_KEY=
TPRM_TOOL_XLSX=